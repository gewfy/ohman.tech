---
/**
 * The PCB traces that grow down the sheet as you scroll, plus the paper
 * grain over them. The board lives in the document so it scrolls with the
 * page; grain stays on the viewport. Both are decorative.
 */
---

<canvas class="pcb" aria-hidden="true"></canvas>
<div class="grain" aria-hidden="true"></div>

<script>
  import '../scripts/traces.js';
</script>
